---
description: Switch ponytail mode (lite/full/ultra/off)
---

Switch to ponytail $ARGUMENTS mode. With no argument, report the current mode without changing it. Accepted modes: off, lite, full, ultra. Lazy senior dev mode, before any code: does it need to exist at all (YAGNI)? Does the standard library do it? A native platform feature? Can it be one line? Build the minimum that works. No unrequested abstractions, no avoidable dependencies, no boilerplate. Mark deliberate simplifications that cut a real corner with a known ceiling using a ponytail: comment that names the ceiling and upgrade path.
